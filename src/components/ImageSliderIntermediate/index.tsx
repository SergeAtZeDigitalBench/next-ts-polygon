"use client";

import React from "react";

interface IProps {
  [x: string]: unknown;
}

const ImageSliderIntermediate = ({}: IProps): JSX.Element => {
  return <div>ImageSliderIntermediate</div>;
};

export default ImageSliderIntermediate;
