import { PageProps } from '@/types'

const EventsPage = async ({ params, searchParams }: PageProps) => {
  return (
    <>
      <h1 className="text-3xl font-bold underline text-center">Events Page</h1>
    </>
  )
}

export default EventsPage
