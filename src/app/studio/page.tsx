import React from "react";

import { IPageProps } from "@/types";

const StudioPage = async ({ params, searchParams }: IPageProps) => {
  return (
    <>
      <h1 className="text-3xl font-bold underline text-center">
        David Kando Studios
      </h1>
    </>
  );
};

export default StudioPage;
