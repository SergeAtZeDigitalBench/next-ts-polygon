import React from "react";

import { IPageProps } from "@/types";

const BandPage = async ({ params, searchParams }: IPageProps) => {
  return (
    <>
      <h1 className="text-3xl font-bold underline text-center">Curcio Band</h1>
    </>
  );
};

export default BandPage;
